Application for find duplicaties:
<pre>
  -h [ --help ]                   Show hint
  -p [ --path ] arg               paths scan
  -e [ --exception ] arg          excluded paths
  -m [ --mask ] arg               masks
  -d [ --depth ] arg (=0)         depth recursion
  -b [ --size_block ] arg (=1024) hashing block size
  -f [ --size_file ] arg (=1)     minimal file size
  -a [ --hash ] arg (=0)          Hash algorithm: 0 - MD5, 1 - CRC32
